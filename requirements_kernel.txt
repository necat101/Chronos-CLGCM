# Core dependencies for running and training. PyTorch 2.4+ supplies the
# fail-closed safe_globals checkpoint-loading boundary used by the active CLI.
torch>=2.4
transformers>=4.30.0
datasets
numpy

# Optional, but highly recommended for fine-tuning, building the C++ kernel,
# and enabling all features.
pybind11
cmake
peft
accelerate
bitsandbytes
numba  # Optional ROSA reference/debug JIT; the persistent production path is pure Python.

# Optional: lm-evaluation-harness for benchmark evaluation during training
# Install with: pip install lm-eval
lm-eval

# Test runner for pre-flight checks
pytest
